import { TableSortDirective } from './table-sort.directive';

describe('TableSortDirective', () => {
	it('should create an instance', () => {
		const directive = new TableSortDirective();
		expect(directive).toBeTruthy();
	});
});
